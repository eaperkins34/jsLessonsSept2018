/*
LITERALS are explicit values, making the value by hand
*/

let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];
console.log(days);
console.log(days.toString());  //cleans up, only shows the values separated by commas

let tired = true;
let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'vegetable',
}

console.log(soup.c);
console.log(soup.a);

let car = 'Honda';

let dec = 10.2;

